<p id="notice"><%= notice %></p>

<%= link_to "Sign Out", destroy_current_member_session_path %>
<%= current_current_member.name %>

<center><h1>Organizations Our Members Are a Part of</h1></center>

<center><p>Here is a list of members and the organizations they are a part of</p></center>

<center>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Organizations</th>
      <th colspan="3"></th>
    </tr>
  </thead>
</center>

<center>
  <tbody>
    <% @member_orgs.each do |member_org| %>
      <tr>
        <td><%= CurrentMember.where(memberID: member_org.memberID).last.name %></td>
        <td><%= CurrentMember.where(memberID: member_org.memberID).last.email %></td>
        <td><%= Organization.where(organizationID: member_org.organizationID).last.name %></td>
        <td><%= member_org.organizationID %></td>
        <td><%= link_to 'View Member Organizations', member_org %></td>
        <td><%= link_to 'Update Member Organizations', edit_member_org_path(member_org) %></td>
        <td><%= link_to 'Remove Member from an Organization', member_org, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</center>

<br>

<center>
<%= link_to 'Add a Member to an Organization', new_member_org_path %>
</center>

<br>

<center>
<%= link_to 'Current Members (Officer View)', current_members_path %>
</center>

<br>

<center>
<%= link_to 'View Organizations', organizations_path %>
</center>